#!/usr/bin/env bash
set -euo pipefail

script_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
printf '%s\n' \
  'clean is build-only until machine boot and storage authority are enrolled.' >&2
exec "$script_dir/build" "$@"
